# Copyright 2011,2012,2016,2018,2019 Free Software Foundation, Inc.
#
# This file was generated by gr_modtool, a tool from the GNU Radio framework
# This file is a part of gr-halow
#
# SPDX-License-Identifier: GPL-3.0-or-later
#

########################################################################
# Setup library
########################################################################
include(GrPlatform) #define LIB_SUFFIX
check_include_file("xmmintrin.h" SSE2_SUPPORTED)

if(SSE2_SUPPORTED)
    set(CMAKE_CXX_FLAGS "${CMAKE_CXX_FLAGS} -msse2")
    add_definitions(-DHALOW_MSSE2)
endif(SSE2_SUPPORTED)
list(APPEND halow_sources
    equalizer/base.cc
    equalizer/comb.cc
    equalizer/halow_ls.cc
    equalizer/lms.cc
    equalizer/ls.cc
    equalizer/sta.cc
    constellations_impl.cc
    halow_frame_equalizer_impl.cc
    halow_sync_long_impl.cc
    halow_sync_short_impl.cc
    mapper_impl.cc
    utils.cc
    )

# use SSE2 optimized viterbi implementation if SSE2 is enabled
if(SSE2_SUPPORTED)
    list(APPEND halow_sources
        viterbi_decoder/viterbi_decoder_x86.cc
    )
else()
    list(APPEND halow_sources
        viterbi_decoder/viterbi_decoder_generic.cc
    )
endif(SSE2_SUPPORTED)

set(halow_sources "${halow_sources}" PARENT_SCOPE)
if(NOT halow_sources)
    MESSAGE(STATUS "No C++ sources... skipping lib/")
    return()
endif(NOT halow_sources)

add_library(gnuradio-halow SHARED ${halow_sources})
target_link_libraries(gnuradio-halow gnuradio::gnuradio-runtime)
target_include_directories(gnuradio-halow
    PUBLIC $<BUILD_INTERFACE:${CMAKE_CURRENT_SOURCE_DIR}/../include>
    PUBLIC $<INSTALL_INTERFACE:include>
  )
set_target_properties(gnuradio-halow PROPERTIES DEFINE_SYMBOL "gnuradio_halow_EXPORTS")

if(APPLE)
    set_target_properties(gnuradio-halow PROPERTIES
        INSTALL_NAME_DIR "${CMAKE_INSTALL_PREFIX}/lib"
    )
endif(APPLE)

########################################################################
# Install built library files
########################################################################
include(GrMiscUtils)
GR_LIBRARY_FOO(gnuradio-halow)

########################################################################
# Print summary
########################################################################
message(STATUS "Using install prefix: ${CMAKE_INSTALL_PREFIX}")
message(STATUS "Building for version: ${VERSION} / ${LIBVER}")

########################################################################
# Build and register unit test
########################################################################
include(GrTest)

# If your unit tests require special include paths, add them here
#include_directories()
# List all files that contain Boost.UTF unit tests here
list(APPEND test_halow_sources
)
# Anything we need to link to for the unit tests go here
list(APPEND GR_TEST_TARGET_DEPS gnuradio-halow)

if(NOT test_halow_sources)
    MESSAGE(STATUS "No C++ unit tests... skipping")
    return()
endif(NOT test_halow_sources)

foreach(qa_file ${test_halow_sources})
    GR_ADD_CPP_TEST("halow_${qa_file}"
        ${CMAKE_CURRENT_SOURCE_DIR}/${qa_file}
    )
endforeach(qa_file)
