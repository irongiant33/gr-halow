# auto-generated by grc.converter

id: halow_halow_frame_equalizer
label: HaLow Frame Equalizer
category: '[halow]'

parameters:
-   id: algo
    label: Algorithm
    dtype: raw
    default: halow.HALOW_LS
-   id: freq
    label: Frequency
    dtype: real
    default: 920.5e6
-   id: bw
    label: Bandwidth
    dtype: real
    default: 10e6
-   id: log
    label: Log
    dtype: bool
    default: 'False'
    options: ['True', 'False']
    option_labels: [Enable, Disable]
-   id: debug
    label: Debug
    dtype: bool
    default: 'False'
    options: ['True', 'False']
    option_labels: [Enable, Disable]

inputs:
-   domain: stream
    dtype: complex
    vlen: 64
    multiplicity: '1'

outputs:
-   domain: stream
    dtype: byte
    vlen: 48
    multiplicity: '1'
-   domain: message
    id: symbols
    optional: true

templates:
    imports: from gnuradio import halow
    make: halow.halow_frame_equalizer(${algo}, ${freq}, ${bw}, ${log}, ${debug})
    callbacks:
    - set_algorithm(${algo})
    - set_frequency(${freq})
    - set_bandwidth(${bw})

file_format: 1