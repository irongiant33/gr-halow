# auto-generated by grc.converter

id: halow_halow_sync_long
label: HaLow Sync Long
category: '[halow]'

parameters:
-   id: log
    label: Log
    dtype: bool
    default: 'False'
    options: ['True', 'False']
    option_labels: [Enable, Disable]
-   id: debug
    label: Debug
    dtype: bool
    default: 'False'
    options: ['True', 'False']
    option_labels: [Enable, Disable]
-   id: sync_length
    label: Sync Length
    dtype: int
    default: '240'

inputs:
-   domain: stream
    dtype: complex
    multiplicity: '1'
-   label: in delayed
    domain: stream
    dtype: complex
    multiplicity: '1'

outputs:
-   domain: stream
    dtype: complex
    multiplicity: '1'
asserts:
- ${ sync_length > 0 }

templates:
    imports: from gnuradio import halow
    make: halow.halow_sync_long(${sync_length}, ${log}, ${debug})

file_format: 1